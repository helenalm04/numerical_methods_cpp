## Data files

This repository includes an example input dataset:

- `tracking_data.dat`: input tracking data (space-separated columns: `t x y`).

Generated file (not committed):
- `speed_data.dat`: produced by running `q2` and used by `q3`.

Tip: you can regenerate `speed_data.dat` at any time by running `./build/q2`.
