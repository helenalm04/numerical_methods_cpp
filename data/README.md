## Data files

Keep coursework datasets out of the public repo unless you are explicitly allowed to share them.

Expected (if your code uses them):
- tracking_data.dat : columns t x y (space-separated)
- speed_data.dat    : generated by q2

Place datasets locally in this folder.
